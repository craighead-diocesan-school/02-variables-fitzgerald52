// ####################################
// #### ----- GST Calculator ----- ####
// ####################################

function gstCalculator() {
  alert('The price with GST the price and 15%.')
let before = prompt('what is the cost before GST ?')
 let GST = before * 1.15
 alert('The cost with GST is $' + GST)
}
